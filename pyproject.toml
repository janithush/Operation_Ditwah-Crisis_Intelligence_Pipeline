[project]
name = "homework"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipython>=9.15.0",
    "jupyter>=1.1.1",
    "langsmith[claude-agent-sdk]>=0.5.0",
    "pandas>=3.0.3",
    "python-dotenv>=1.2.1",
]
